// DashboardLayout component converted from styled-components to plain HTML elements

function DashboardLayout({ children, ...props }) {
  return (
    <div 
      data-component="dashboard-layout"
      {...props}
    >
      {children}
    </div>
  );
}

export default DashboardLayout;